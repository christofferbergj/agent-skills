interface:
  display_name: "Bro"
  short_description: "Restate messages in plain, concise language"
  default_prompt: "Use $bro to restate your last message in plain, concise language."
policy:
  allow_implicit_invocation: false
